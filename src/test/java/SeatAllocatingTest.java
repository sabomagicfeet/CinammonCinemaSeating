import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class SeatAllocatingTest {

    @Disabled
    @Test
    public void printAvailableSeatsTest() {
        SeatAllocating seatAllocating = new SeatAllocating();

//        String expected = "A1, A2, A3, A4, A5, B1, B2, B3, B4, B5, C1, C2, C3, C4, C5";
//        String actual = seatAllocating.printAvailableSeats();
//
//        Assertions.assertEquals(expected, actual);

    }
}
